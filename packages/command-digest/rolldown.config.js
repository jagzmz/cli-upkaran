import { createConfig } from '../../rolldown.config.base.js';

export default createConfig({
  packageDir: 'packages/command-digest',
  // Add any package-specific externals here if needed
  external: [], 
}); 